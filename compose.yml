# ============================================================
# airis-monorepo
# ============================================================
# Generated by `airis gen` - DO NOT EDIT MANUALLY
# Source of truth: manifest.toml
#
# Use `airis up` to start. Profiles control which services run.
# ============================================================

services:
  postgres:
    container_name: airis-monorepo-postgres
    image: postgres:16-alpine
    volumes:
    - ws_node_modules_root:/app/node_modules
    - ws_pnpm_root:/pnpm/virtual-store
    - ws_pnpm-store_root:/pnpm/store
  redis:
    container_name: airis-monorepo-redis
    image: redis:7-alpine
    volumes:
    - ws_node_modules_root:/app/node_modules
    - ws_pnpm_root:/pnpm/virtual-store
    - ws_pnpm-store_root:/pnpm/store
networks:
  default:
    name: airis-monorepo_default
    external: false
  traefik:
    name: traefik_default
    external: true
volumes:
  ws_node_modules_root: null
  ws_pnpm_root: null
  ws_pnpm-store_root: null
