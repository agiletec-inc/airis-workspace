# ============================================================
# airis-monorepo
# ============================================================
# Generated by `airis gen` - DO NOT EDIT MANUALLY
# Source of truth: manifest.toml
#
# Use `airis up` to start. Profiles control which services run.
# ============================================================

services:
  postgres:
    container_name: airis-monorepo-postgres
    image: postgres:16-alpine

  redis:
    container_name: airis-monorepo-redis
    image: redis:7-alpine


networks:
  default:
    name: airis-monorepo_default
    external: false
  traefik:
    name: traefik_default
    external: true

volumes:
